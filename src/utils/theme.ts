import { createMuiTheme } from '@material-ui/core/styles';

// obtained from https://material-ui.com/customization/default-theme/
const theme = createMuiTheme({
  palette: {
    type: 'dark',
  },
});

export default theme;
